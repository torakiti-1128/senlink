<VirtualHost *:80>
    ServerName localhost

    ProxyPreserveHost On
    
    # 転送先をDocker内のAPIコンテナ（core-api）のポート8080に向ける
    ProxyPass / http://core-api:8080/
    ProxyPassReverse / http://core-api:8080/

    # 転送プロトコルを明示的に付与
    RequestHeader set X-Forwarded-Proto "http"
</VirtualHost>
